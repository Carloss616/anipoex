import type { HeroUINativeConfig } from "heroui-native/provider";

/** Native runs HeroUI's defaults — see [the web override](./config.web.ts). */
export const toastConfig: HeroUINativeConfig["toast"] = undefined;
