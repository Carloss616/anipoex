// Two client IDs because AniList allows one Redirect URL per registered app.
export const CLIENT_ID = process.env.EXPO_PUBLIC_ANILIST_CLIENT_ID_NATIVE;

export const REDIRECT_URI = "anipoex://auth";
